import { describe, expect, it } from 'vitest'
import { <%= name %> } from './index.js'

describe("测试", () => {
  it.each([[1, 1, 2],[1, 2, 3]])
  ("<%= name %>",
   (param1, param2, expected) => {
    expect(<%= name %>(param1, param2)).toEqual(expected);
  });
});
